---
"apiuikit": minor
"@apiuikit/web-component": minor
---

Add `config.sidePanel.containment` (`"component"` | `"viewport"`) so SidePanel overlays can either clip to the widget's root element or cover the full browser viewport. Default is `"component"`, which changes prior full-viewport-height overlay behavior for embeds and section components.
