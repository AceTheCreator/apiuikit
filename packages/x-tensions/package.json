{
  "name": "x-tensions",
  "private": true,
  "version": "0.0.0",
  "description": "Internal catalog of API spec-extension (x-*) renderers, bundled into apiuikit at build time. Never published on its own.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "peerDependencies": {
    "react": ">=18"
  }
}
